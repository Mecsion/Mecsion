# -*- coding:utf-8 -*-

#有一个字符串为“星期一星期二星期三星期四星期五星期六星期日”，编写程序，
#当从键盘上输入数字1时屏幕输出“星期一”，输入数字2时屏幕输出“星期二”，以此类推。
newString="星期一星期二星期三星期四星期五"
start=eval(input("write your number please:"))
realstart = start*3-1
print(newString[realstart-2:realstart+1])

#编写Python程序，按下列要求完成操作。
#输入字符串“http://sports.sina.com.cn/”， 输出以下结果：
#① 字符串中字母t出现的次数。
#② 字符串中“com”子字符串出现的位置。
#③ 将字符串中的所有的“.”替换为“-”。
#④ 将字符串中的字母全变为大写。
#⑤ 输出字符串中的总字符个数。
#⑥ 在字符串后拼接字符串“index”

string="http://sports.sina.com.cn/"
print("字符串中t出现的次数为：%d"%string.count("t"))
print("字符串中com出现的位置的角标为:%d"%string.find("c"))
new_string=string.replace(".","-")
print("将字符串中的.全部改为-后的字符串为%s"%new_string)
upper_string=string.upper()
print("将字符串中字母全部改为大写后的字符串为%s"%upper_string)
print ("该字符串的字符总个数为%d"%len(string))
index_string=string+"index"
print("字符串与index拼接后为%s"%index_string)
